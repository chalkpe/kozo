import type { Tab } from '../types/ui'

/** 탭 목록 */
export const tabs: { name: string; value: Tab }[] = [
  { name: 'All', value: 'all' },
  { name: 'Chat', value: 'chat' },
  { name: 'System', value: 'system' },
]
